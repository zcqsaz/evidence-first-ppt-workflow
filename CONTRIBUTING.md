# Contributing

感谢参与改进 Evidence-First PPT Workflow。

## 贡献原则

1. 新规则应来自可复现问题，而不是个人审美偏好；
2. 每项强制规则必须给出检查方法或可观察的通过条件；
3. 示例不得包含未授权论文图、机构模板、真实用户文件或个人信息；
4. 自动检查器不得静默修改PPTX，只能报告问题；
5. 破坏兼容性的Schema或目录变更必须记录在 `CHANGELOG.md`；
6. 文档中的“必须、禁止、否决”应有明确适用范围，避免不可执行的绝对表述。

## 提交流程

1. 创建Issue并描述问题、复现条件、实际后果和期望规则；
2. 修改文档、模板或代码；
3. 为代码修改补充或更新测试；
4. 运行：

```bash
python -m pytest -q
python -m compileall scripts
python scripts/validate_pptx.py --help
python scripts/validate_sources.py --help
```

5. 在Pull Request中说明：
   - 修改解决了什么实际问题；
   - 是否改变阶段产物或验收阈值；
   - 是否存在迁移步骤；
   - 使用了哪些测试文件，是否具有公开许可。

## 文档风格

- 首次出现的术语给出定义；
- 用可验证的动作代替“更高级、更美观、更专业”等模糊描述；
- 规则优先写成“输入—动作—产物—门禁”；
- 中英文文件名、字段名和命令保持一致；
- 不在文档中提交本机绝对路径、账号、密钥或内部网址。
